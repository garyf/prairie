# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    attributes:
      custom_field:
        gist: 'The'
  controllers:
    flash:
      create:
        failure: "Failed to create %{entity}"
        success: "%{entity} successfully created"
      destroy:
        success: "%{entity} successfully destroyed"
      update:
        failure: "Failed to update %{entity}"
        success: "%{entity} successfully updated"
  helpers:
    search_results:
      count:
        zero: "Search results"
        one: "1 search result"
        other: "%{count} search results"
      multiple_pages: "%{entry_name} %{first} - %{last}"
      single_page:
        zero: "No %{entry_name} found"
        one: "1 %{entry_name} found"
        other: "All %{count} %{entry_name} found"
