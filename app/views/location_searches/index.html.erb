<ul class='nav  nav-pills'>
  <li class='active'><a><%= search_results_count %></a></li>
  <li><%= link_to 'New search for locations', new_location_search_path %></li>
</ul>

<% if @search_results_for_page.any? %>
  <h5><%= search_results_info @results_count %></h5>
  <ul class=unstyled><%= render partial: 'location', collection: @search_results_for_page %></ul>
  <%= render partial: 'common/search_pagination_nav' %>

<% else %>
  <h5>No results found</h5>
<% end %>
